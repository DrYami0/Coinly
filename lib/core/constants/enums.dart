enum TransactionType { expense, income, transfer }

enum AccountType { cash, bank, card }

enum RecurrenceType { daily, weekly, biweekly, monthly, yearly }

enum GoalStatus { active, completed, paused, archived }

enum IncomeFrequency { weekly, biweekly, monthly, yearly }
